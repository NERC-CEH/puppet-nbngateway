#!/bin/bash

now=`date +"%Y_%m_%d_%H_%M"`
logdir="<%= @directory %>"

mkdir -p $logdir

find /home/tomcat7/*/logs/* -type f -mtime +1 -print0 | tar -czf "$logdir/tomcat-$now.tgz" --remove-files --null -T -
